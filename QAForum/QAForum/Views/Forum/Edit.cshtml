@model QAForum.Models.ViewModels.ForumViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary("You have the following errors")
    <fieldset>
        <legend>Forum</legend>

        @Html.HiddenFor(model => model.ForumId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ForumTitle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ForumTitle)
            @Html.ValidationMessageFor(model => model.ForumTitle)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
